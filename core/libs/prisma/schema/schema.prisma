// Main schema configuration file
// This is the entry point for the multi-file Prisma schema

generator client {
  provider   = "prisma-client-js"
  output     = "../generated/client"
  engineType = "binary"
}

datasource db {
  provider = "mysql"
}
